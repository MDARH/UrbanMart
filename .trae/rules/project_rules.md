Make sure to keep note on ChangeLog for every file changes.
## Commenting Rules for Mohammad Hassan

- Every change made to any file **must** include a comment with my name:  
  `// Mohammad Hassan`  
  or  
  `# Mohammad Hassan`  
  depending on the file type.

- Place the comment **immediately above or beside** the changed line(s).

- For multi-line changes, add the comment **once at the top** of the change block.

- Example:  
  ```php
  // Mohammad Hassan
  $newVariable = 'value';
  ```

- Keep the comment **short and consistent**—no additional text is needed.

- This rule applies to **all languages** and **all file types** in the project.
